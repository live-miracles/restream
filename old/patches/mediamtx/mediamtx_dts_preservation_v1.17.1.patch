diff --git a/internal/protocols/mpegts/from_stream.go b/internal/protocols/mpegts/from_stream.go
index 4840d73..2c293ae 100644
--- a/internal/protocols/mpegts/from_stream.go
+++ b/internal/protocols/mpegts/from_stream.go
@@ -59,6 +59,7 @@ func FromStream(
 				track := &mcmpegts.Track{Codec: &tscodecs.H265{}}
 
 				var dtsExtractor *h265.DTSExtractor
+				var videoStarted bool
 
 				addTrack(
 					media,
@@ -69,17 +70,31 @@ func FromStream(
 							return nil
 						}
 
-						if dtsExtractor == nil {
+						if !videoStarted {
 							if !h265.IsRandomAccess(u.Payload.(unit.PayloadH265)) {
 								return nil
 							}
-							dtsExtractor = &h265.DTSExtractor{}
-							dtsExtractor.Initialize()
+							videoStarted = true
 						}
 
-						dts, err := dtsExtractor.Extract(u.Payload.(unit.PayloadH265), u.PTS)
-						if err != nil {
-							return err
+						var dts int64
+						var err error
+
+						if u.HasDTS {
+							dts = u.DTS
+						} else {
+							if dtsExtractor == nil {
+								if !h265.IsRandomAccess(u.Payload.(unit.PayloadH265)) {
+									return nil
+								}
+								dtsExtractor = &h265.DTSExtractor{}
+								dtsExtractor.Initialize()
+							}
+
+							dts, err = dtsExtractor.Extract(u.Payload.(unit.PayloadH265), u.PTS)
+							if err != nil {
+								return err
+							}
 						}
 
 						sconn.SetWriteDeadline(time.Now().Add(writeTimeout))
@@ -98,6 +113,7 @@ func FromStream(
 				track := &mcmpegts.Track{Codec: &tscodecs.H264{}}
 
 				var dtsExtractor *h264.DTSExtractor
+				var videoStarted bool
 
 				addTrack(
 					media,
@@ -110,17 +126,31 @@ func FromStream(
 
 						idrPresent := h264.IsRandomAccess(u.Payload.(unit.PayloadH264))
 
-						if dtsExtractor == nil {
+						if !videoStarted {
 							if !idrPresent {
 								return nil
 							}
-							dtsExtractor = &h264.DTSExtractor{}
-							dtsExtractor.Initialize()
+							videoStarted = true
 						}
 
-						dts, err := dtsExtractor.Extract(u.Payload.(unit.PayloadH264), u.PTS)
-						if err != nil {
-							return err
+						var dts int64
+						var err error
+
+						if u.HasDTS {
+							dts = u.DTS
+						} else {
+							if dtsExtractor == nil {
+								if !idrPresent {
+									return nil
+								}
+								dtsExtractor = &h264.DTSExtractor{}
+								dtsExtractor.Initialize()
+							}
+
+							dts, err = dtsExtractor.Extract(u.Payload.(unit.PayloadH264), u.PTS)
+							if err != nil {
+								return err
+							}
 						}
 
 						sconn.SetWriteDeadline(time.Now().Add(writeTimeout))
diff --git a/internal/protocols/mpegts/to_stream.go b/internal/protocols/mpegts/to_stream.go
index 434dd17..8010626 100644
--- a/internal/protocols/mpegts/to_stream.go
+++ b/internal/protocols/mpegts/to_stream.go
@@ -45,11 +45,17 @@ func ToStream(
 				}},
 			}
 
-			r.OnDataH265(track, func(pts int64, _ int64, au [][]byte) error {
-				pts = td.Decode(pts)
+			r.OnDataH265(track, func(rawPTS int64, rawDTS int64, au [][]byte) error {
+				dts := td.Decode(rawDTS)
+				pts := td.Decode(rawPTS)
+				if dts > pts {
+					dts = pts
+				}
 
 				(*subStream).WriteUnit(medi, medi.Formats[0], &unit.Unit{
-					PTS:     pts, // no conversion is needed since clock rate is 90khz in both MPEG-TS and RTSP
+					PTS:     pts,
+					DTS:     dts,
+					HasDTS:  true,
 					Payload: unit.PayloadH265(au),
 				})
 				return nil
@@ -64,11 +70,17 @@ func ToStream(
 				}},
 			}
 
-			r.OnDataH264(track, func(pts int64, _ int64, au [][]byte) error {
-				pts = td.Decode(pts)
+			r.OnDataH264(track, func(rawPTS int64, rawDTS int64, au [][]byte) error {
+				dts := td.Decode(rawDTS)
+				pts := td.Decode(rawPTS)
+				if dts > pts {
+					dts = pts
+				}
 
 				(*subStream).WriteUnit(medi, medi.Formats[0], &unit.Unit{
-					PTS:     pts, // no conversion is needed since clock rate is 90khz in both MPEG-TS and RTSP
+					PTS:     pts,
+					DTS:     dts,
+					HasDTS:  true,
 					Payload: unit.PayloadH264(au),
 				})
 				return nil
diff --git a/internal/protocols/rtmp/from_stream.go b/internal/protocols/rtmp/from_stream.go
index eee7d57..fe63767 100644
--- a/internal/protocols/rtmp/from_stream.go
+++ b/internal/protocols/rtmp/from_stream.go
@@ -115,6 +115,7 @@ func FromStream(
 					tracks = append(tracks, track)
 
 					var videoDTSExtractor *h265.DTSExtractor
+					var videoStarted bool
 
 					r.OnData(
 						media,
@@ -124,17 +125,31 @@ func FromStream(
 								return nil
 							}
 
-							if videoDTSExtractor == nil {
+							if !videoStarted {
 								if !h265.IsRandomAccess(u.Payload.(unit.PayloadH265)) {
 									return nil
 								}
-								videoDTSExtractor = &h265.DTSExtractor{}
-								videoDTSExtractor.Initialize()
+								videoStarted = true
 							}
 
-							dts, err := videoDTSExtractor.Extract(u.Payload.(unit.PayloadH265), u.PTS)
-							if err != nil {
-								return err
+							var dts int64
+
+							if u.HasDTS {
+								dts = u.DTS
+							} else {
+								if videoDTSExtractor == nil {
+									if !h265.IsRandomAccess(u.Payload.(unit.PayloadH265)) {
+										return nil
+									}
+									videoDTSExtractor = &h265.DTSExtractor{}
+									videoDTSExtractor.Initialize()
+								}
+
+								var err error
+								dts, err = videoDTSExtractor.Extract(u.Payload.(unit.PayloadH265), u.PTS)
+								if err != nil {
+									return err
+								}
 							}
 
 							nconn.SetWriteDeadline(time.Now().Add(writeTimeout))
@@ -159,6 +174,7 @@ func FromStream(
 					tracks = append(tracks, track)
 
 					var videoDTSExtractor *h264.DTSExtractor
+					var videoStarted bool
 
 					r.OnData(
 						media,
@@ -182,21 +198,35 @@ func FromStream(
 								}
 							}
 
-							// wait until we receive an IDR
-							if videoDTSExtractor == nil {
+							if !videoStarted {
 								if !idrPresent {
 									return nil
 								}
-
-								videoDTSExtractor = &h264.DTSExtractor{}
-								videoDTSExtractor.Initialize()
-							} else if !idrPresent && !nonIDRPresent {
-								return nil
+								videoStarted = true
 							}
 
-							dts, err := videoDTSExtractor.Extract(u.Payload.(unit.PayloadH264), u.PTS)
-							if err != nil {
-								return err
+							var dts int64
+
+							if u.HasDTS {
+								dts = u.DTS
+							} else {
+								// wait until we receive an IDR
+								if videoDTSExtractor == nil {
+									if !idrPresent {
+										return nil
+									}
+
+									videoDTSExtractor = &h264.DTSExtractor{}
+									videoDTSExtractor.Initialize()
+								} else if !idrPresent && !nonIDRPresent {
+									return nil
+								}
+
+								var err error
+								dts, err = videoDTSExtractor.Extract(u.Payload.(unit.PayloadH264), u.PTS)
+								if err != nil {
+									return err
+								}
 							}
 
 							nconn.SetWriteDeadline(time.Now().Add(writeTimeout))
diff --git a/internal/recorder/format_fmp4.go b/internal/recorder/format_fmp4.go
index f573e85..f6a377b 100644
--- a/internal/recorder/format_fmp4.go
+++ b/internal/recorder/format_fmp4.go
@@ -335,6 +335,7 @@ func (f *formatFMP4) initialize() bool {
 				track := addTrack(forma, codec)
 
 				var dtsExtractor *h265.DTSExtractor
+				var videoStarted bool
 
 				f.ri.reader.OnData(
 					media,
@@ -378,17 +379,31 @@ func (f *formatFMP4) initialize() bool {
 							f.updateCodecParams()
 						}
 
-						if dtsExtractor == nil {
+						if !videoStarted {
 							if !randomAccess {
 								return nil
 							}
-							dtsExtractor = &h265.DTSExtractor{}
-							dtsExtractor.Initialize()
+							videoStarted = true
 						}
 
-						dts, err := dtsExtractor.Extract(u.Payload.(unit.PayloadH265), u.PTS)
-						if err != nil {
-							return err
+						var dts int64
+						var err error
+
+						if u.HasDTS {
+							dts = u.DTS
+						} else {
+							if dtsExtractor == nil {
+								if !randomAccess {
+									return nil
+								}
+								dtsExtractor = &h265.DTSExtractor{}
+								dtsExtractor.Initialize()
+							}
+
+							dts, err = dtsExtractor.Extract(u.Payload.(unit.PayloadH265), u.PTS)
+							if err != nil {
+								return err
+							}
 						}
 
 						var sampl fmp4.Sample
@@ -418,6 +433,7 @@ func (f *formatFMP4) initialize() bool {
 				track := addTrack(forma, codec)
 
 				var dtsExtractor *h264.DTSExtractor
+				var videoStarted bool
 
 				f.ri.reader.OnData(
 					media,
@@ -454,17 +470,31 @@ func (f *formatFMP4) initialize() bool {
 							f.updateCodecParams()
 						}
 
-						if dtsExtractor == nil {
+						if !videoStarted {
 							if !randomAccess {
 								return nil
 							}
-							dtsExtractor = &h264.DTSExtractor{}
-							dtsExtractor.Initialize()
+							videoStarted = true
 						}
 
-						dts, err := dtsExtractor.Extract(u.Payload.(unit.PayloadH264), u.PTS)
-						if err != nil {
-							return err
+						var dts int64
+						var err error
+
+						if u.HasDTS {
+							dts = u.DTS
+						} else {
+							if dtsExtractor == nil {
+								if !randomAccess {
+									return nil
+								}
+								dtsExtractor = &h264.DTSExtractor{}
+								dtsExtractor.Initialize()
+							}
+
+							dts, err = dtsExtractor.Extract(u.Payload.(unit.PayloadH264), u.PTS)
+							if err != nil {
+								return err
+							}
 						}
 
 						var sampl fmp4.Sample
diff --git a/internal/recorder/format_mpegts.go b/internal/recorder/format_mpegts.go
index 7eaafb9..2b27fd7 100644
--- a/internal/recorder/format_mpegts.go
+++ b/internal/recorder/format_mpegts.go
@@ -88,6 +88,7 @@ func (f *formatMPEGTS) initialize() bool {
 				track := addTrack(&tscodecs.H265{})
 
 				var dtsExtractor *h265.DTSExtractor
+				var videoStarted bool
 
 				f.ri.reader.OnData(
 					media,
@@ -99,17 +100,31 @@ func (f *formatMPEGTS) initialize() bool {
 
 						randomAccess := h265.IsRandomAccess(u.Payload.(unit.PayloadH265))
 
-						if dtsExtractor == nil {
+						if !videoStarted {
 							if !randomAccess {
 								return nil
 							}
-							dtsExtractor = &h265.DTSExtractor{}
-							dtsExtractor.Initialize()
+							videoStarted = true
 						}
 
-						dts, err := dtsExtractor.Extract(u.Payload.(unit.PayloadH265), u.PTS)
-						if err != nil {
-							return err
+						var dts int64
+
+						if u.HasDTS {
+							dts = u.DTS
+						} else {
+							if dtsExtractor == nil {
+								if !randomAccess {
+									return nil
+								}
+								dtsExtractor = &h265.DTSExtractor{}
+								dtsExtractor.Initialize()
+							}
+
+							var err error
+							dts, err = dtsExtractor.Extract(u.Payload.(unit.PayloadH265), u.PTS)
+							if err != nil {
+								return err
+							}
 						}
 
 						return track.write(
@@ -130,6 +145,7 @@ func (f *formatMPEGTS) initialize() bool {
 				track := addTrack(&tscodecs.H264{})
 
 				var dtsExtractor *h264.DTSExtractor
+				var videoStarted bool
 
 				f.ri.reader.OnData(
 					media,
@@ -141,17 +157,31 @@ func (f *formatMPEGTS) initialize() bool {
 
 						randomAccess := h264.IsRandomAccess(u.Payload.(unit.PayloadH264))
 
-						if dtsExtractor == nil {
+						if !videoStarted {
 							if !randomAccess {
 								return nil
 							}
-							dtsExtractor = &h264.DTSExtractor{}
-							dtsExtractor.Initialize()
+							videoStarted = true
 						}
 
-						dts, err := dtsExtractor.Extract(u.Payload.(unit.PayloadH264), u.PTS)
-						if err != nil {
-							return err
+						var dts int64
+
+						if u.HasDTS {
+							dts = u.DTS
+						} else {
+							if dtsExtractor == nil {
+								if !randomAccess {
+									return nil
+								}
+								dtsExtractor = &h264.DTSExtractor{}
+								dtsExtractor.Initialize()
+							}
+
+							var err error
+							dts, err = dtsExtractor.Extract(u.Payload.(unit.PayloadH264), u.PTS)
+							if err != nil {
+								return err
+							}
 						}
 
 						return track.write(
diff --git a/internal/unit/unit.go b/internal/unit/unit.go
index 9703791..598e407 100644
--- a/internal/unit/unit.go
+++ b/internal/unit/unit.go
@@ -10,9 +10,15 @@ import (
 
 // Unit is an atomic unit of a stream.
 type Unit struct {
-	// relative time
+	// relative time (presentation timestamp)
 	PTS int64
 
+	// relative time (decode timestamp, from sources that carry it like MPEG-TS)
+	DTS int64
+
+	// whether DTS was provided by the source
+	HasDTS bool
+
 	// absolute time
 	NTP time.Time
